{
  "name": "tropass-agent-kit",
  "version": "0.1.0",
  "private": true,
  "description": "MCP connector kit for using Tropass ML models from AI agents and developer tools.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "smoke": "node scripts/smoke-check.mjs",
    "lint": "npm --workspace packages/mcp-proxy run lint",
    "test": "npm --workspace packages/mcp-proxy run test",
    "check": "npm --workspace packages/mcp-proxy run check && node --check scripts/smoke-check.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "typescript-eslint": "^8.48.0",
    "vitest": "^4.0.14"
  }
}
